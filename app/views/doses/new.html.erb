<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :description %>
  <%= f.input :ingredient_id, collection: @avialable_ingredients, value_method: :id, label_method: :name %>
  <%= f.submit %>
<% end %>
